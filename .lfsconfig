[lfs]
    ; Avoid pushing to github LFS accidentally
    ; If LFS is set up correctly, it will use the lfs-dal transer agent, the url will be ignored
    url = "lfs-dal-is-not-being-used-FORGOT-TO-SOURCE-GIT-CONFIG"
